
    static long counter跟踪所创建的Shared的实例数量，还可以为id提供数值。
    counter的类型是long而不是int,这样可以防止溢出。
    id是final的，因为我们不希望它的值在对象声明周期中被改变。


    在将一个共享对象附着到类上时，必须记住调用addRef(),但是dispose()方法将跟踪引用数，并决定何时执行清理。
    使用这种技巧需要加倍的细心，但是如果如果你正在共享需要清理的对象，那么你就没有太多的选择余地了。